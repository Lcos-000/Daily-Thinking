
```markdown
# 多模型共享单卡时的显存配额与抢占

## 问题场景

一张 GPU 上部署两个模型：

- **模型 A**：业务优先级高，流量稳定，有严格的 SLA 要求
- **模型 B**：业务优先级低，流量波动大，可接受一定程度的性能降级

两者共享同一张 GPU 显存，各自维护独立的 Continuous Batching 队列和 KV Cache 池。

**触发事件**：模型 B 流量突增，其 KV Cache 占用持续增长，挤压模型 A 的可用显存。A 的并发能力从 40 降到 15，请求开始排队，延迟上升。

**核心问题**：如何设计两个模型间的显存配额机制，既不让 B 完全无资源可用，又能保证 A 不被 B 挤压到无法满足业务优先级要求？

---

## 思考链路

### 第一反应：加权轮询？

> 可以参照多级队列和防饥饿机制。对高优先级始终优先保证，通过加权轮询或计时的方式保证 B 任务不饥饿。

**纠正**：这个方向摸到了"防饥饿"这个大类，但把 Day 18 的"请求排队策略"直接搬了过来，混淆了两种不同性质的问题。

- **加权轮询**管的是**时间维度**上的公平（谁先被服务），前提是资源可以被时分复用（如 CPU 时间片）
- 但**显存是空间维度的资源**，一旦被某个请求的 KV Cache 占用，物理上就是被占着的，不存在"轮询着用"的说法

关键追问：显存该怎么在 A、B 间分配——更接近"谁先被服务"（时间调度问题）还是"谁能拿到多少空间"（配额/容量划分问题）？如果是后者，加权轮询是否适用？

---

### 方案一：固定划分（被淘汰）

> 是不是把一部分显存固定分给 A、固定分给 B？

**测试这个方案**：

| 场景 | 现象 | 问题 |
|------|------|------|
| 深夜 B 几乎无流量 | B 专用 block 大量空闲，A 遇流量高峰、专用 block 用满还有请求排队 | A 无法使用 B 空闲的部分 |
| B 流量暴增 | B 专用 block 用满大量排队，A 流量正常、专用 block 只用了一半 | B 无法借用 A 空闲的部分 |

固定划分在两种场景下都造成了显存利用率的浪费，违背了"资源不能被浪费"的效率目标。

**追问**：有没有一种方案，既能保证 A 的最低保障不被无限挤压，又能让双方在各自不需要时把空闲资源让给对方——配额不是一条死的分界线，而是一个"有底线、但可以动态借用"的机制？

---

### 方案二：弹性配额（采纳）

> 直接弹性配额。划分弹性空间和最大截止空间，保证另一方的最低限度。

**落地为具体参数**：

给 A、B 各自设定 `min`（最低保障）和 `max`（最高上限）：

| 模型 | min（最低保障） | max（最高上限） |
|------|----------------|----------------|
| A | 300 | 800 |
| B | 100 | 500 |

> 注：两者 max 之和（800+500=1300）可超过总量（如 1000），因为不会同时都用满上限。

**机制**：
- 平时谁需要就往外扩，最多扩到自己的 max
- 不管对方扩得多厉害，也不能挤压到另一方的 min 以下
- 这解决了固定划分的两个场景问题

**尚未考虑的细节**：保底空间若暂时未被保底方使用，是否允许被临时借用？

---

### 实现层面的关键问题

> 这个 min/max 的判断和分配，具体是**谁**来做、**什么时候**做？

**候选方案**：

| 方案 | 描述 | 风险 |
|------|------|------|
| **A：中心化** | 独立的显存配额管理器持续监控双方占用，一旦要突破 max 或挤压对方 min 就主动介入——拒绝新分配或主动驱逐已有请求 | 单点？但可控 |
| **B：去中心化** | A、B 各自的调度器在尝试分配新 block 时检查全局占用情况，发现会超限就自己主动拒绝或触发驱逐 | 多个决策方同时对同一份共享资源做判断和修改——经典并发问题 |

**判断**：需要中心化维护状态，包括用原子锁之类的防止死锁或类似事故。

**理由**：多个决策方（A、B 各自的调度器）若各自独立判断"我能不能分配"、再各自修改同一份共享状态（全局显存占用），正是 Day 1、Day 6、Day 9 反复出现的**经典并发问题**：多个并发写方若没有统一的原子仲裁点，会出现**竞态**——A、B 几乎同时检查"未到上限"都通过，同时分配后加起来超限或挤压对方的 min。这与 Day 9 的 Gang Scheduling 死锁问题（多个任务各自"以为自己能拿到资源"，实际加起来超了）是同一个模式。

---

### 最终方案确认

- **配额策略**：弹性配额（min + max，允许动态借用）
- **实现方式**：中心化显存配额管理器 + 原子操作仲裁

核心原则：任何跨多方共享资源的配额判断，都不能让多个决策方各自独立判断，必须收敛到一个原子的仲裁点。

---

## 方法论沉淀

### 1. 辨清资源的物理性质

面对"资源如何在多方之间公平分配"这类问题，首先要辨清资源本身的性质：

| 资源类型 | 特征 | 适用策略 |
|----------|------|----------|
| 时间型资源（CPU 时间片、带宽） | 可被时分复用 | 轮询、排队、时间片调度 |
| 空间型资源（显存、磁盘容量） | 一旦分配就物理独占，直到释放前都不可共享 | 配额、容量划分、弹性借用 |

**教训**：用错误性质的调度思路（如把时间调度的公平机制套用到空间资源分配上）看似合理，实则文不对题。

---

### 2. 固定划分 vs 弹性配额：折中的必要性

| 方案 | 优点 | 缺点 |
|------|------|------|
| 固定划分 | 简单、边界清晰、绝对隔离 | 牺牲整体利用率，任何时候一方空闲、一方紧张就会造成浪费 |
| 完全共享 | 利用率最大 | 高优先级任务可能被挤爆，无保障 |
| **弹性配额（min + max）** | 在底线之上允许动态借用，兼顾保障与利用率 | 实现复杂度稍高 |

弹性配额的本质：在"绝对隔离（避免互相挤压）"和"完全共享（追求最大利用率）"之间找一个可调节的折中点。

---

### 3. 共享资源分配必须收敛到原子仲裁点

涉及**多个独立决策方共享同一份有限资源**（显存、锁、队列名额等）的场景，配额或准入判断必须：

- ✅ 收敛到一个**原子的仲裁点**
- ❌ 不能让各决策方各自独立检查、独立决定

这是贯穿以下多个场景的**共同底层原则**：

| Day | 场景 | 核心问题 |
|-----|------|----------|
| Day 1 | 并发编程 | 多个线程同时修改共享变量 |
| Day 6 | 分布式锁 | 多个进程争抢同一把锁 |
| Day 9 | Gang Scheduling | 多个任务各自以为能拿到资源，实际加起来超了 |
| Day 22 | 显存配额 | A、B 各自的调度器同时做分配决策 |

**实现手段**：全局锁、Redis Set + Lua 脚本（原子"检查+分配"）、或专用的配额管理器组件。

---

## 一句话总结

> **用弹性配额（min + max）做空间维度的容量划分，用中心化原子仲裁做并发维度的决策收敛——两者缺一不可。**
```