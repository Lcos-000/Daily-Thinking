```markdown
# 多模型共享单卡时的公平 GPU 时间片调度

> 场景：模型 A 与模型 B 共享同一张 GPU，同一时刻只能执行一个模型的 forward 计算。  
> A 单次 forward 耗时 50ms，B 单次 forward 耗时 200ms。  
> 业务目标：按优先级比例（如 A:B = 7:3）分配 GPU 时间。

---

## 1. 常见误区：把“轮流调度次数”当成“公平分配”

若调度器采用简单策略——**轮流执行 A 的一个 step、B 的一个 step**：

- 跑 10 轮（A、B 各跑 10 次）：
  - A 总占用 = 10 × 50ms = 500ms
  - B 总占用 = 10 × 200ms = 2000ms
  - 总耗时 = 2500ms
  - A 实际占比 = 20%，B 实际占比 = 80%

**结论**：  
“轮流调度”控制的是 **被调度的次数相等**，而非 **实际占用的 GPU 时间相等**。  
当单次操作耗时差异巨大时，次数公平 ≠ 时间公平。

若业务要求 A:B = 7:3 的时间占比，这种“按次数轮流”的方式**完全无法达成目标**。

---

## 2. 正确的方向：按“实际 GPU 时间配额”调度

调度器应转向 **按实际时间片配额分配**，而非按次数轮询。

### 核心机制：追踪“累积缺口”

调度器维护两个累加器：

- `actual_A`：A 到目前为止实际累积占用的 GPU 时间
- `actual_B`：B 到目前为止实际累积占用的 GPU 时间

每次调度决策时：

1. 计算此刻按目标比例应占用的时间：
   - `target_A = total_time × 70%`
   - `target_B = total_time × 30%`
2. 计算缺口：
   - `deficit_A = target_A - actual_A`
   - `deficit_B = target_B - actual_B`
3. 将当前时间片分配给 **缺口更大（更“亏”的一方）**

> 示例：  
> 某一刻，A 实际占比 68%（缺口 2%），B 实际占比 32%（超标 -2%）。  
> 缺口 A > 缺口 B，本次机会给 A。  
> 但给 A 后其占比可能变为 70.5%，此时 B 更亏，下一次机会可能轮给 B。  
> 这是一个持续、动态的“追赶”过程，不存在永久拉黑。

---

## 3. 为什么这种方式能解决“单次耗时差异巨大”的问题？

- A 单次耗时短（50ms），每次分配后填补的缺口小，因此会被 **更频繁地调度** 以追上目标。
- B 单次耗时长（200ms），每次分配后填补的缺口大，因此调度次数自然减少。
- 最终效果：**实际 GPU 时间占比收敛于目标比例（如 7:3）**，而非次数比例。

这与“按次数轮流”的本质区别在于：  
**调度单位从“次数”变为“真实资源消耗量（时间）”**。

---

## 4. 该机制的理论归属

这套机制是调度理论中经典算法的实际应用：

- **Weighted Fair Queuing（WFQ）**  
  按权重分配服务时间，持续追踪每个队列的累积服务量，每次选择累积服务量最少的队列。

- **Deficit Round Robin（DRR）**  
  为每个队列维护一个“赤字计数器”，每次轮询时根据配额（量子）增加赤字，若赤字足以发送一个包则允许发送，否则累积至下一轮。

两者的共同核心思想都是：  
**用“累积亏欠量”而非“当前状态”或“历史次数”，来决定下一次调度对象**。

---

## 5. 方法论沉淀

1. **明确公平维度**  
   设计调度策略前，必须先厘清业务目标衡量的是 **次数公平** 还是 **资源消耗公平**。当单次操作成本差异显著时，两者完全不等价，必须明确选择。

2. **用“缺口驱动”代替“静态判定”**  
   正确机制是持续追踪实际 vs 目标的累积缺口，每次将资源分配给缺口最大者。  
   这是一种 **动态自我修正的追赶过程**，不存在“一次超标就永久排除”的逻辑。

3. **原则复用**  
   此思路与 Day 18 的 token 配额分配原则一致——配额应按 **真实资源消耗量**（token 数、GPU 时间）而非“个数”或“次数”分配。  
   本案例是该原则在 **时间维度资源** 上的具体应用。

---

## 附：决策逻辑伪代码

```python
# 初始化
actual_A = 0.0   # ms
actual_B = 0.0   # ms
total_time = 0.0 # ms
weight_A = 0.7
weight_B = 0.3

while True:
    # 若有请求到达
    if request_A and request_B:
        target_A = total_time * weight_A
        target_B = total_time * weight_B
        deficit_A = target_A - actual_A
        deficit_B = target_B - actual_B
        
        if deficit_A >= deficit_B:
            run_A()      # 耗时 50ms
            actual_A += 50
        else:
            run_B()      # 耗时 200ms
            actual_B += 200
        
        total_time += (50 or 200)
    # 若只有一方有请求，直接执行（无竞争时无需调度）
    elif request_A:
        run_A()
        actual_A += 50
        total_time += 50
    elif request_B:
        run_B()
        actual_B += 200
        total_time += 200
```

> 注意：实际系统中需考虑时间片粒度、请求到达模式、量化误差等工程细节，但核心逻辑如上。
