# Day 28 思考链路记录：多副本间的模型版本一致性

## 原题

5 个副本跑同一个模型服务，做 A/B 测试——2 个副本切换成新版本模型权重，3 个保持旧版本，负载均衡器随机分发流量。多轮对话场景中，用户第一轮被路由到新版本副本，生成的回复基于新版本权重；若第二轮因负载均衡策略或粘性路由失效被路由到旧版本副本，旧版本模型看到的对话历史里会包含一段"由新版本模型生成"的回复。这种"同一对话跨越不同模型版本"的情况会带来什么具体问题？该怎么设计路由机制避免这个问题？

---

## 第一反应

> 采用会话粘性路由，要求同一会话绑定一个副本，这样可以复用 KV Cache，模型也不会变。可以采用打标签的办法，比如采用了 V1 模型就始终采用有 V1 模型的副本。

---

## 追问 1（要求区分两种粘性的优先级性质）

认可这个方向能同时解决 KV Cache 复用和模型版本一致性两个问题，但指出这次说的"粘性路由"跟此前讨论粘性路由时确立的"优先级"存在关键不同，需要讲清楚区别。给出此前的结论：粘性路由不是凌驾于负载均衡之上的独立规则，而是"在已筛出的、都可以接受的候选副本里，优先选有缓存的那个"——如果拥有缓存的副本已经因为水位太高被淘汰，就该放弃粘性，选别的副本，接受"缓存作废、重新 Prefill"的代价。

给出具体冲突场景：用户第二轮对话原本应粘性路由到副本 A（v1 模型，有这轮对话的 KV Cache 缓存），但副本 A 水位已经很高（快接近拒绝阈值）。按"放弃粘性、选别的副本"的规则，如果"别的副本"里只有跑 v2 模型的副本还有空间，路由器会不会把请求发给 v2 副本？如果发生这种情况，"用户对话历史混入不同版本模型生成的内容"这个后果，跟"KV Cache 缓存作废、重新 Prefill"这个后果（多算一点、多花一点延迟），是不是同一个量级的问题？追问更直接的对比："缓存作废"付出的代价是性能上的（结果依然正确，只是慢一点）；"模型版本混用"付出的代价是正确性上的（用户可能得到逻辑不连贯、风格突变、质量下降的回复，这是用户能直接感知的产品体验问题）。这两种代价能不能用同一条"优先级里可以被放弃"的规则来处理？

---

## 回答

> 这两个是不同层面的。KV Cache 的粘性因为水位上涨是可以丢弃的，但模型版本的粘性不能容忍——如果没有可用的同版本副本，应该扩容或拒绝，因为这是一个错误，而不是一个可以容忍的情况。

---

## 确认并落地为完整架构

判断准确，精确识别出两种"粘性"背后完全不同的性质——**KV Cache 粘性是一个"优化项"（放弃了只是变慢，结果依然正确），模型版本粘性是一个"正确性约束"（放弃了会产生错误的结果，不可接受）**。这两者不能用同一条"优先级里可以妥协"的规则处理，模型版本这条线应该是一票否决式的硬约束。

### 落地为分层过滤的路由架构

- **第一层（模型版本，硬性过滤，不可妥协）**：判断这个对话历史里已经用过哪个版本的模型。若已用过 v1，这一轮的候选副本只能从"跑 v1 模型的副本"里选，v2 副本直接从候选列表排除，不管 v2 副本有多空闲——一票否决，没有"降级妥协"这个选项。

- **第二层（在"只跑 v1 的副本"这个子集内，再套用已有的负载均衡与粘性规则）**：KV Cache 水位硬性拒绝（排除真正扛不住的）、队列长度打分、以及 KV Cache 缓存粘性（优先选之前处理过这段对话的那个 v1 副本，如果它水位也扛得住）。

### 兜底处理

如果"跑 v1 模型的副本"这一层过滤下来，全部都扛不住（水位都高、队列都长），此时不能降级去用 v2，只能：
- 让请求排队等 v1 副本腾出空间；
- 或触发扩容（给 v1 模型多起一个副本）；
- 或在极端情况下直接拒绝这次请求（把决定权交给客户端重试）——

但绝对不能"随便找个 v2 副本顶上去糊弄过去"。

---

## 方法论沉淀

当系统中同时存在多种"粘性/亲和性"需求时，不能默认它们都属于同一优先级、可以用同一套"放弃即可"的降级规则处理——必须先判断每种粘性背后对应的代价性质：

- 如果放弃后果是"性能下降但结果依然正确"（如缓存作废、重新计算），它属于可以在资源紧张时被牺牲的**软目标/优化项**；

- 如果放弃后果是"结果本身出错、影响正确性或产品体验的一致性"（如模型版本混用导致对话前后不一致），它必须被当作**硬约束**对待，需要独立成一道不可绕过的过滤层，宁可触发排队、扩容或拒绝，也不能为了保证系统可用而悄悄突破这条底线。