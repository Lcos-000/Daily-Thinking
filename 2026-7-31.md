```markdown
# 流式推理请求客户端断开后的资源回收

## 场景描述

LLM 推理服务通过 gRPC 流式接口返回生成结果，服务端在循环中逐 token 生成并通过 `stream.Send` 推送给客户端。若用户在生成中途关闭浏览器或网络断开，服务端需要及时感知并回收资源，避免无效计算和显存占用。

## 核心问题

1. 客户端断开后，服务端多久能感知到？
2. 感知延迟会造成什么样的资源浪费？
3. 如何设计让服务端尽快感知并做出正确反应？

## 解决方案剖析

### 1. 利用框架自带生命周期信号（而非自建心跳）

在 gRPC 流式接口中，`stream.Context()` 返回的 context 与连接生命周期绑定：

```go
// 典型的 gRPC 流式 handler
func (s *server) Generate(req *pb.Request, stream pb.LLM_GenerateServer) error {
    ctx := stream.Context()  // 连接断开时 Done() 会被关闭
    
    for {
        // 每轮生成前检查客户端是否还在
        select {
        case <-ctx.Done():
            return ctx.Err()  // 客户端已断开，立即退出
        default:
        }
        
        token, err := s.decodeNextToken(ctx)  // 耗时的 GPU 计算
        if err != nil {
            return err
        }
        if err := stream.Send(&pb.Token{Text: token}); err != nil {
            return err  // Send 失败也会退出
        }
        if token == stopToken {
            return nil
        }
    }
}
```

**关键认知**：
- 框架已提供现成的生命周期信号（`stream.Context().Done()`），无需自建心跳协议
- 检查时机：**每次循环开始时检查**，确保每轮 decode 前先确认连接状态

### 2. Continuous Batching 场景下的特殊考量

在 Continuous Batching 系统（Day 14）中，多个请求被合并成 batch 做一次 forward 计算：

```
┌──────────────────────────────────────────────────────┐
│ Batch (30 个请求)                                    │
│  ┌────────────────────────────────────────────────┐  │
│  │  一次 GPU forward 计算（不可中断）             │  │
│  │  耗时 ~50ms                                   │  │
│  └────────────────────────────────────────────────┘  │
│                    ↓                                 │
│  分发结果给每个请求：                                │
│    ├── 请求 A: 正常生成 token 并 Send               │
│    ├── 请求 B: 检测到 ctx.Done() → 移除            │
│    └── 请求 C: 正常生成 token 并 Send               │
└──────────────────────────────────────────────────────┘
```

**核心限制**：
- 一次 forward 计算（矩阵乘法）一旦在 GPU 上启动就**不可中断**，也无法中途将某个请求踢出
- 即使请求 B 在 forward 开始前已检测到 `ctx.Done()`，也无法避免这次 forward 中包含 B 的计算

### 3. 浪费量化与优化效果

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 方案对比                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│ 无检测机制：                                                                │
│   └── 客户端断后，继续生成直到 done=true → 浪费 N 次 forward（N 可达数百）│
│                                                                             │
│ 每次循环检查 ctx.Done()：                                                   │
│   └── 最多浪费 1 次 forward 计算（当前这一轮）                             │
│       即：把浪费上限从 "整个剩余生成过程" 压缩到 "一次 forward 耗时"       │
│       (通常 20-80ms，取决于模型规模)                                       │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4. 完整的资源回收流程

```
检测到 ctx.Done()
        ↓
  退出当前 for 循环
        ↓
  移除请求的 KV Cache Block（Day 16/23 的回收机制）
        ↓
  释放显存，供 batch 内其他请求或队列中新请求使用
```

**重要提醒**：
- 仅停止生成还不够，必须主动释放该请求占用的 KV Cache（显存资源）
- 若不回收，已断开的请求会持续占用显存，导致可用资源不断减少

### 5. 双重保障：兜底超时机制

```go
// 除了连接检测，增加绝对超时防止异常情况
ctx, cancel := context.WithTimeout(stream.Context(), 5*time.Minute)
defer cancel()

for {
    select {
    case <-ctx.Done():
        return ctx.Err()  // 超时或连接断开均触发
    default:
    }
    // ... decode & send
}
```

**设计理由**：
- 细粒度检测（每轮检查 ctx）：正常情况下的主要手段，零成本（channel + select）
- 粗粒度超时（兜底）：防止 ctx 因某些异常未被正确关闭，导致资源无限占用

## 方法论沉淀

### 原则 1：优先使用框架现成的生命周期信号

> 处理"请求/连接生命周期感知"类问题时，应当优先审视所使用的框架/协议本身是否已经提供了现成的生命周期信号（如 gRPC 的 `stream.Context()`），而不是想当然地自己重新设计一套探测机制（如心跳协议）。

自建心跳往往是不必要的重复劳动——底层框架已经解决了连接感知问题，只需在正确的位置消费这个信号即可。

### 原则 2：理解"信号检测"与"不可中断操作"的共存模式

> 当一个"检测信号"（如 ctx 取消）与一个"不可中断的批量操作"（如一次 forward 计算）共存时，检测信号能够生效的最快时机，天然受限于这个不可中断操作的边界。

不能指望检测到信号就能立刻生效，而应该：
1. 清楚地计算"最坏情况下会延迟多久、造成多大的浪费"
2. 通过在合适的边界点（如每个 step 开始前）持续检测
3. 把浪费的上限从"整个未完成的过程"压缩到"一个最小操作单元"的量级

这是一种普遍适用于批处理系统中"如何优雅响应外部取消信号"的设计模式。
